{
  "name": "webcleaner",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "lint": "prettier --check .",
    "prepare": "git config core.hooksPath .githooks || true"
  },
  "devDependencies": {
    "prettier": "latest"
  }
}
